import 'package:flutter/material.dart';

class DetailPay extends StatelessWidget {
  const DetailPay({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
